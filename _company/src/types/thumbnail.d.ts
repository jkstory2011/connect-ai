export interface LayerConfig {
  background: string; // HEX
  textColor: string;
  overlayIcon?: string;
}

export interface ThumbnailProps {
  title: string;
  subtitle: string;
  imageUrl: string;
}